# 開発作業メモ

Milipro Chronicle（ミリクロ）の開発メモ。作業するたびに追記していく。

## 2026-08-21（33）

### タレント定義の一元化＋配信ダンジョンの「推し選択」を戦闘へ反映（正しさ・穴是正）
- 背景: 企画書・作業メモ・各ソースの監査で、正しさの穴として①タレント定義の3箇所重複 ②配信ダンジョンで「推しを選ぶ」が演出のみ（戦闘へ影響なし）③音楽力の獲得経路なし が判明。ユーザー指示により③は別ゲームで対応のため見送り、①②をブラッシュアップ
- **タレント定義の一元化**:
  - 単一ソース = `MILLIPRO_TALENTS`（firebase-init.js、他サイトへコピペする共有コードのためここを正とする）。各10人に `group`（null/nova/uni）と `battle:{atk,def}` を追加（atk型=+15%atk/-10%def / def型=-10%atk/+20%def / balance=等倍。±15%程度で勝率維持）
  - `script.js`: `const TALENTS = {...}` → `var TALENTS = MILLIPRO_TALENTS`（index.htmlは firebase-init→script の順で読込済み）
  - `dungeon.js`: `var DUNGEON_TALENTS = {...}` → `var DUNGEON_TALENTS = MILLIPRO_TALENTS`
  - `dungeon.html`: `<script src="firebase-config.js">` / `firebase-init.js` を gameData.js の前に追加（dungeon別ページでも単一ソースを参照。firebase-initはtop-levelでfirebaseを呼ばず安全）
  - 結果: タレントの追加・変更は1箇所だけで済む（従来は3箇所）
- **配信ダンジョンの推し反映**:
  - `talentBattleStats(level)` → `talentBattleStats(level, talentId)` に変更（gameData.js）。`MILLIPRO_TALENTS[talentId].battle` があれば base の atk/def に倍率を適用（最低1ガード、MILLIPRO_TALENTS未定義時はbaseにフォールバック）
  - 呼び出し4箇所を更新（dungeon.js: startBattleの`talentId`、renderBattle/doCommand/doGiftの`state.talentId`）
  - 型分け: atk型=あくび・らこ・りず・れい / def型=ゆら・こま・つくり / balance=このみ・のの・ぬふ（作業メモの小ネタから推定。差は控えめで推奨Lvの勝率100%は維持）
- **検証**:
  - `node --check` 全JS通過 / CSS均衡OK（style.css 691/691, dungeon.css 188/188）
  - 既存3テスト全合格（oshitest 31 / resettest 21 / canvastest 36）に加え、新規 `tests/talenttest.js` **97 pass**（単一ソースの10人分・参照化・HTML読込順・シグネチャ・4箇所呼出・挙動差分）
  - 挙動: Lv10で atk型37/9・def型29/12・balance32/10。未知ID/nullはbaseにフォールバック

---

## 2026-08-19（31）

### ドキュメント更新: 連携系の新実装（§27〜30）に合わせて整合（コード変更なし）
- 依頼: 「連携系を大幅に変更してきたのでコードは触らず変えてください」→ ドキュメントのみ更新。コードは一切変更していない
- 検証: `tests/` 3スイート全て合格（oshitest 31 / resettest 21 / canvastest 36）。`firebase-rules.json` と連携ハンドオフ §6 のルールが一致していることを確認
- **連携ハンドオフ.md の修正**:
  - §0: 「認証は使わない（開発初期はルール全開放）」が現状（Auth 使用 + `firebase-rules.json` 適用済み）と矛盾していたため、ルール適用状況・イベント書き込みは未ログイン可の説明に修正
  - §2-3 / §8: 他サイトが最新版 `firebase-init.js` を丸コピーして `completeMilliproLogin`（**gamedata 同期付き**）を呼ぶとクラウドのセーブデータを上書きし得るため、「他サイトは `ensureMilliproProfile` → `applyMilliproProfile` の2段階版に差し替えること」という注意を明記
- **現状確認で見つかった既知の課題（記録のみ・修正はユーザー判断）**: 2026-08-17 のモバイル欠陥修正（コミット b08c56e）は連携系の再整理に伴いほぼ全面巻き戻し。現行コードには以下が残っている:
  - 痛バッグのタイマーが「←戻る」・タブ切替・他ポップアップを開く経路では止まらない（popup-itabag の背景クリック/閉じるボタン経由でのみ停止。script.js の popup-itabag 直リスナー）
  - ホームタブがサブ画面スタック中（例: プレイヤーカード → ジョブ）だと1回でホームへ戻らない（`closePopup()` は1つ前の画面へ pop するだけ）
  - クエスト報酬ダイアログが `lv.level` 参照（`addExp` の戻り値は `{ leveledUp, newLevel }`）→ レベルアップ行が `Lv.undefined` になる恐れ（script.js:1696）
  - `firebase.auth().currentUser.email` の裸参照（script.js:1259。firebase 未ロード時に例外の恐れ）
  - ダンジョン: 名前表示が `user.name` のまま（`playerName` 未対応）、`startBattle` で `startCommentFlow` が再開されない、立ち絵フォールバック時の `portrait` クラスが選択画面チップを崩す、`#app` の `100dvh` なし、モバイルタップ領域42px以下
  - pwa.js: 強制バナー（X/LINE/Instagram 内ブラウザ）で閉じるボタンが非表示 → ゲームロックの恐れ
  - external.js: 「同一ゲーム1時間1回」が同一バッチ内で効かない（`gameLastClaimed` が非同期更新のため）、並行同期時の read-modify-write 競合、`playedAt=0` のイベントが永久スキップ
  - firebase-init.js: `syncMilliproGameData` の戻り値が `'pushed' : 'pushed'`（冗長。`'created'` の意図）、`newPlayerIdFallback` の `crypto` 裸参照
- **→ 2026-08-19 に全ファイル復元済み（下記エントリ32）**

## 2026-08-19（32）

### モバイル欠陥修正（b08c56e）の全ファイル復元（連携系の新実装は維持）
- 状況: エントリ31 の記録後、ユーザー確認の結果「ファイルを確認せずに入れ替えてしまった可能性」と判明。全ファイルを最善状態まで確認・復元
- **巻き戻しファイルを b08c56e 版（最善版）に完全復元**: dungeon.js / dungeon.css / pwa.js / external.js（ユーザーの新規変更を含まないため丸ごと戻し）
- **新実装（oshi 共有・パスワード再設定・キャンバス☰メニュー）を保持しつつ修正を再適用**:
  - script.js: `stopItabagGame()` 復活（openPopup/closePopup/ホームタブ経由で必ず停止）、ホームタブ即戻り復活、`closePopup` の戻り先再描画（プレイヤーカード/ジョブのみ・ギャラリー等は描画しない）、痛バッグ縦スワイプはスクロールとして扱う修正、クエスト報酬 `lv.newLevel`、事務所 `stage` ガード、`(firebase.auth().currentUser || {}).email`、`loadGalleryAll` の firebaseAvailable ガード + タブ切替競合ガード + `.catch` エラー表示、`syncGallerySales` を `gallerySalesQueue` で直列化
  - firebase-init.js: `firebaseAvailable` に `typeof firebase.database === 'function'` ガード、`crypto` は `typeof` ガード、`syncMilliproGameData` の戻り値 `'pushed' : 'created'`
  - style.css: `.pwa-overlay.forced` でも閉じるボタンを表示（ゲームロック防止）
- **テスト側の対応**: resettest.js のスタブ firebase に `database` スタブを追加（`firebaseAvailable` が database ガードを持つようになったため）
- 検証: `node --check` 全JS通過 / CSS均衡OK / 回帰テスト3スイート全合格（oshitest 31 / resettest 21 / canvastest 36）

---

## 2026-08-17（30）

### Firebase RTDB のセキュリティルール整備（「安全でないルール」警告メールへの対応）
- **背景**: テストモードの全開放ルール（`.read/.write: true`）のままで Firebase から警告メールが届いていた。`firebase-rules.json` を新規作成して安全なルールを定義
- **ルールの内容**:
  - ルートは全面拒否。`millipro/` のみ許可
  - `users/{uid}/**`: **ログイン本人のみ**読み書き（profile / gamedata）
  - `watchEvents` / `gameEvents`: Unishare/Games が**未ログインで書き込む**ため書き込みは許可するが、**値の形式を検証**（watchedAt/durationSec、score/playedAt が数値であること。追加フィールドは許容）。書き込みは最下層ノードのみ
  - `rewards` / `gallery` / `gallerySales`: 本アプリ（ログイン必須）が書くため **auth != null** ＋形式検証（grantedAt / 作品必須フィールド / price・at 等）
- **適用方法**: Firebase コンソール → Realtime Database → ルールタブに `firebase-rules.json` の内容を貼り付けて公開（ユーザー作業）。適用後は警告メールが止まる
- **連携ハンドオフ.md**: §6 のルール欄を新ルールに差し替え（設計理由・メール対応の説明付き）、§7 に「書き込みは最下層ノードへ・数値フィールド」の注意を追記
- **修正（27fd203）**: 初回公開時に「No such method/property 'childrenCount'」エラー（rewards / gallerySales の2箇所）。RTDB ルールに `childrenCount()` は存在しないため除去して再公開 → **公開成功・警告メール停止**（2026-08-17 確認）
- コード変更なし（読み書きパスはルールと整合していることを確認済み: script.js / external.js / firebase-init.js）

---

## 2026-08-15（29）

### 最推し/推しを全サイト共通プロフィールで共有（他サイトから読み書き可能に）
- **データ設計**: 共有プロフィール `millipro/users/{uid}/profile` に `ultimateOshi`（タレントID・1人）と `favorites`（ID配列・最大10人）を追加。フィールド単位 `update()` なので他サイトの名前/アイコン更新と競合しない
- **firebase-init.js（共有コード・他サイトがそのまま利用）**:
  - `MILLIPRO_TALENTS` 定数（10人の ID+名前。全サイト共通ID）
  - `getMilliproOshi()`（ローカルから取得）/ `updateMilliproOshi(ult, favs)`（ローカル+クラウド保存・不正ID除去・10人上限）
  - `ensureMilliproProfile` がローカルの oshi をクラウドへバックフィル、`applyMilliproProfile` がクラウドの oshi をローカルへ反映
- **本アプリ UI**:
  - `popup-oshi` 新設: 最推し（10人から1人選択）＋ 推し（最大10人複数選択・カウント表示）。保存でローカル+クラウドへ
  - プレイヤーカード「プロフィール編集」に「💖 推し設定を変更」ボタン追加
  - 初回ウィザード確定（`confirmSetup`）時もログイン中ならクラウドへ push
- **連携ハンドオフ.md**: oshi フィールド仕様・ID一覧・読み出し/保存サンプル（初回アカウント作成時の設定UI例を含む）を追記。Unishare の推し専用UIはそちら側の自由設計と明記
- 検証: `node --check` OK、CSS 691/691 均衡、canvastest 36/36、resettest 21/21、**oshitest（新規）31 pass**（取得/保存/上限/不正ID/バックフィル/反映）

---

## 2026-08-15（28）

### パスワード再設定機能を追加（全サイト共通アカウント対応）
- **firebase-init.js**: `milliproResetPassword(email)` を追加（共有コード。他サイトも同じ関数を使える）。`sendPasswordResetEmail(email, { url: 自サイトオリジン + '/', handleCodeInApp: false })` で、どのサイトから送っても共通アカウントの再設定ができる。リセット後の戻り先は呼び出し元サイト
- **本アプリ UI**:
  - ログインゲート（`login-gate`）と連携画面（`popup-external`）の両方に「パスワードをお忘れですか？」リンクを追加
  - `#password-reset-dialog`（モーダル: メール入力 → 送信 → 成功/エラー表示）を追加。z-index 9999999（ゲート 999999 の前面）
  - `openPasswordResetDialog` / `closePasswordResetDialog` / `submitPasswordReset` / `resetPasswordError` を実装。ログイン欄にメールが入っていれば引き継ぐ。Enter 送信・外側クリックで閉じる
  - エラー文言: 未登録 / 形式不正 / 試行回数超過 を日本語化
- **連携ハンドオフ.md**: `milliproResetPassword` の説明と他サイト向け実装例（`mpResetPassword` + リンク追加）を追記
- 検証: `node --check` OK、CSS 679/679 均衡、canvastest 36/36、resettest（新規・送信内容/ダイアログ/関数）**21 pass**
- 回帰テストを `/tmp/opencode` から **`tests/` に永続化**（環境リセットで消えるため）

---

## 2026-08-14（27）

### キャンバスお絵かきツールをハンバーガーメニュー化（1画面に収めて描きやすく）
- 旧レイアウト（ツールバー/色/太さ/アクションの縦積み）を廃止し、**トップバー＋☰ハンバーガーメニュー＋常時表示カラースウォッチ**の構成に刷新
- **トップバー**: ☰ボタン / 現在ツール表示（✏️ ペン・🧽 消しゴム）/ クイック切替（✏️・🧽）＋🗑️クリア
- **☰メニュー**（ドロップダウン）: ツール（ペン/消しゴム）・太さ（3/6/12、サイズドット付き）・アクション（📁画像を選ぶ/💾端末に保存/🖼️投稿）。選択後は自動で閉じ、外側クリックでも閉じる
- **色**: 12色スウォッチを常時表示（選ぶと自動でペンに切替）
- **キャンバス**: `.canvas-wrap` を flex:1 中央寄せにし、`aspect-ratio:1` + `max-width:min(100%,640px)` + `max-height:100%` で残りスペースに収まる正方形に（描画座標は従来どおり rect スケールで512px換算）
- `setCanvasTool()` でクイック・メニュー両方の active 表示とステータスを同期
- 検証: `node --check` OK、CSS 668/668 均衡、`/tmp/opencode/canvastest.js`（メニュー開閉/ツール切替/色/太さ/保存/投稿/画像読込/外側クリック）**36 pass / 0 fail**

---

## 2026-08-14（26）

### 痛バッグ制作にドラッグ&ドロップを追加（PC: HTML5 drag / スマホ: タッチドラッグ）
- パレットのバッジを**ドラッグでスロットに配置**できるように（従来のタップ選択＋タップ配置もそのまま動作）
- **配置済みバッジはスロット間をドラッグで移動**できる（PC: ドラッグ中に drop-target ハイライト表示 / スマホ: 指に追従するゴースト表示、移動量10px以上でドラッグ開始→スクロール停止）
- 実装（script.js）: `itabagPlaceBadge(targetIdx, badgeId, sourceIdx)` で配置/移動判定。`itabagDrag` 状態＋ドキュメント共通 touchmove/touchend/touchcancel（passive:false）でタッチドラッグを処理。パレットボタンと配置済みスロットに `draggable="true"`
- style.css: `.itabag-drag-ghost`（追従ゴースト）/ `.drop-target`（ハイライト）/ `.dragging`（半透明）
- テスト: `/tmp/opencode/itabagdragtest.js`（配置/埋まり無視/移動/範囲外）**5 pass / 0 fail**、`node --check` OK、CSS 658/658 均衡

---

## 2026-08-14（25）

### 痛バッグ制作画面のレイアウト改善（「バッグがデカすぎて見づらい」対応）
- `.itabag-bag` に `max-width: 240px; margin: 0 auto` を追加して全幅3×3（aspect-ratio 1）の巨大化を解消。バッジ絵文字 28→22px
- `.itabag-order`（注文）も `max-width: 260px; margin: 0 auto` でバッグと揃える
- パレットボタンをコンパクト化（絵文字 22→18px、min-width 60→52px）
- **幅560px以上では2カラム化**（左: 注文+バッグ / 右: パレット+操作ボタン）で縦スクロールを減らす（`@media (min-width: 560px)` + grid）
- 検証: CSS 655/655 均衡、gardentest 31/31、tabtest 36/36

---

## 2026-08-14（24）

### 記憶の庭＋記憶の欠片を実装（§11 / §15）
- **データ**（gameData.js、`GARDEN_SHARDS`/`GARDEN_AREAS`/`GARDEN_CULTIVATE_REWARDS`/`GARDEN_COMPLETE_REWARDS`、`GAME_DATA_VERSION` を 6 に）
  - 記憶の欠片10種（`GARDEN_SHARDS`）: 初配信/初勝利/応援/初グッズ/創作/事務所/図鑑/ブレイク/伝説/完走。各 `cond(gd)` で既存進行（動画視聴・ダンジョン・ギャラリー・グッズ・事務所段階・図鑑完全制覇・Lv）から獲得
  - エリア6種（`GARDEN_AREAS`）: デビューの丘/配信の森/歌みたの泉/周年の塔/記憶の花畑/図書館（事務所第4段階で解放）。各3記憶（計18、実データのミリプロ歴史）
  - `gardenSweepShards`（欠片スイープ）/ `gardenCultivate`（開拓・知識消費）/ `gardenPlant`（欠片を所持していると植えられる。欠片は消費しない）/ `claimGardenAreaReward`（エリア完成: 200通貨/100EXP/📣30 初回のみ）/ `claimGardenCompleteReward`（完全制覇: 1000通貨/500EXP/📣150/📚100 初回のみ）
  - 欠片は未消費コレクション（図鑑の「解読で知識消費」とは逆で、持っていれば植え放題）
- **UI**（script.js / style.css / index.html）
  - `renderGardenScreen()`: サマリ（開拓数/記憶数/知識）→ 所持欠片チップ → エリアカード（未開拓=開拓ボタン / 開拓済み=記憶スロット。欠片所有なら「植える」ボタン、未所有なら取得条件表示）→ 星見台/Chronicle Gate（準備中ティーザー）
  - 新欠片・開拓成功・記憶が咲く・エリア完成・完全制覇をゲームダイアログで通知
  - `popup-garden` 追加、PCアプリ「🌳 記憶の庭」追加（デスクトップは図鑑の隣）
  - 出典注記: ミリプロ公式サイト・非公式wiki（2026年8月時点）
- **テスト**
  - `node --check`（script.js / gameData.js）構文OK、CSS 645/645 均衡
  - `/tmp/opencode/gardentest.js`: 欠片スイープ/開拓/植える/エリア報酬/完全制覇 **31 pass / 0 fail**
  - `/tmp/opencode/tabtest.js`: popup-garden の遷移を追加し **36 pass / 0 fail**

---

## 2026-08-14（23）

### 図鑑の中身を実データ（公式・非公式wiki）に差し替え
- 依頼: 「今の中身もう入れてない？ 入れてないOR仮なら 2サイト（非公式wiki / 公式サイト）から取得し、入れ込みやすいものを入れて。ゲームの進捗風＋小ネタが欲しい」
- 情報収集結果
  - 非公式wiki（https://wikiwiki.jp/millipro10/）は取得OK。公式サイト（https://milpr.com/）はJS主体で本文取得不可 → **非公式wikiを主ソース**に採用
  - 確定事実: 2023/4/1 事務所創設（甘狼このみ＋ミリちゃん、このみ=0期生兼クリエイター）/ 1期生 音ノ乃のの 2023/6/3 デビュー / NOVA（らこ・ゆら・ぬふ）/ UNI（リズ・ツクリ 2025/5/17 デビュー、レイ 2026/7/11 加入で10人に）/ 1st 3D LIVE「Million Story」2026/5/9 Zepp Shinjuku / 全体曲「Mile Stone」
  - 小ネタ: このみ=完全セルフ受肉＋口癖「えへへ」/ のの=紀文公認「ちくわ代表」1日8袋 / ツクリ=マルチクリエイター（作曲歌編集イラスト）/ レイ=朝に5度寝・ごはんとおかず同時食べ
- **gameData.js の `ZUKAN_DEFS` を全面差し替え**（id・`cond()` は維持、title/how/text を実データに）
  - quotes: 公式キャッチコピー・紹介文の一節（「可能性を最大限に、もっと飛躍する。」等）
  - streams: このみ初配信 / Million Story / Mile Stone / レイ初配信 / 20万人耐久歌枠リレー
  - trivia: セルフ受肉 / ちくわ代表 / マルチクリエイター / えへへ / 朝に弱いスナイパー
  - history: 創設→1期生→NOVA→UNI→10人目（事務所段階に連動）
  - memoryCards: デビュー / 1万人達成 / メジャーデビュー / 3Dお披露目 / 1st 3D LIVE
- script.js: 図鑑フッターに「出典: ミリプロ公式サイト・非公式wiki（2026年8月時点）」を明記
- テスト: `node --check` OK、`/tmp/opencode/zukantest.js` 20/20 PASS（条件変更なしのため既存ロジックは維持）

---

## 2026-08-14（22）

### 図鑑システムを実装（§12: 発見 → 解読 → 完全解放）
- ホームの本棚（これまで飾り）をクリックで図鑑を開けるように。PCデスクトップにも「📚 図鑑」アプリを追加
- **データ**（gameData.js）
  - `ZUKAN_CATEGORIES`: 名言 / 神配信 / 小ネタ / 歴史 / 記憶カード の5種
  - `ZUKAN_DEFS`: 各5項目（計25）。各項目に `cond(gd)` の発見条件（Lv・ダンジョン勝利・痛バ完成・グッズ購入・事務所拡張・動画視聴等の既存進行を参照）
  - 状態 `s`: 0=未発見（？？？）/ 1=発見（タイトルのみ）/ 2=解読（詳細解放）
  - `zukanSweepDiscoveries`: 既存データから条件達成分をまとめて発見し **知識ポイント+5/件** を付与（スキーマに `zukan.completed` 追加、`GAME_DATA_VERSION` を 5 に）
  - `zukanDecode`: **知識10**を消費して詳細を解放（`ZUKAN_DECODE_COST`）
  - `zukanCategoryProgress` / `claimZukanCategoryReward`: カテゴリ全解読で **300通貨/150EXP/📣50** を初回のみ付与
- **UI**（script.js / style.css / index.html）
  - `renderZukanScreen(cat)`: 全体サマリ → カテゴリタブ（解読数/総数）→ 項目カード。未発見は「？？？？？🔒」、発見は解読ボタン、解読済は詳細＋発見条件表示
  - 新発見はゲームダイアログで通知（知識獲得表示）、解読成功／完全制覇もダイアログ表示
  - `popup-zukan` 追加（サブ画面。戻るでホーム／元のタブへ復帰）
  - 図鑑専用CSS（白カード・タブ・完了バナー・解読ボタン）を高級感ポリッシュに合わせて追加
- **テスト**
  - `node --check`（script.js / gameData.js）構文OK、CSS 614/614 均衡
  - `/tmp/opencode/zukantest.js`: 図鑑ロジック20項目（発見/知識/解読/完全制覇報酬）ALL PASS
  - `/tmp/opencode/tabtest.js`: 図鑑の画面遷移を追加し **31 pass / 0 fail**
  - 備考: 発見条件の詳細は `def.how` に表示。動画視聴系は外部連携（Milli Unishare）の `videosWatched` に連動

---

## 2026-08-14（21）

### モバイル対応: ダブルタップ拡大の無効化と狭幅最適化
- **ダブルタップ拡大の制限**（ユーザー要望）
  - `style.css` / `dungeon.css` 共通で `html, body { touch-action: manipulation; -webkit-tap-highlight-color: transparent; }` を追加
  - ダブルタップ拡大と 300ms の遅延を無効化。**ピンチズームは維持**（アクセシビリティ考慮）。必要なら viewport に `user-scalable=no` 追加で全面無効化も可
  - 既存の `touch-action: none`（お絵かきキャンバス / BGMスライダー）は交差計算によりそのまま有効
- **iPhone ホームインジケータ対応**
  - `#tab-bar` を `height:auto` + `min-height:60px` + `padding-bottom: env(safe-area-inset-bottom)` に
  - `.popup-overlay` / `#home-screen` の下部余白を `calc(60px + env(safe-area-inset-bottom))` に（タブバーに隠れる問題を防止）
- **狭幅スマホ（≤400px）のショップカード**
  - `.shop-card-foot` を縦積みに、`.shop-card-buy` を全幅ボタン化（価格との横並び潰れ・ボタンが小さすぎ問題を解消）
- **検証**: CSS波括弧 均衡（style.css 592/592、dungeon.css 187/187）。実機でのタップ可否・見た目は要確認

---

## 2026-08-14（20）

### UI全体の高級感ポリッシュ＋ショップをオンラインショップ風に再設計
- ユーザー要望: 各アプリのUIを見直して高級感を出し、ストアを「ミリプロのオンラインショップ」として見やすく整える
- **ショップ（Milli Pro オンラインショップ）再設計**
  - `renderShopScreen()` を全面刷新: ヒーローバナー（Milli Shopロゴ＋「公式グッズで、推し活をもっと楽しく。」）→ 所持通貨ウォレット → カテゴリ別（アクセサリー/痛バグッズ/ルームグッズ）の商品カード2列グリッド
  - `SHOP_CATS`（カテゴリ定義）/ `SHOP_THUMB`（品目別サムネイル背景グラデーション）を追加
  - 商品カード: カラーサムネ・名称・説明（2行省略）・価格タグ・「購入する / 通貨不足」ボタン・ホバーで浮上、所持数バッジ
- **共通基盤**: `.popup-body` に背景グラデーション、カード（`.popup-card` ほか）を白背景＋影＋角丸14px、セクションタイトルを「左アクセントバー＋字間空き」の統一スタイルに（gallery / player-card / office / quest）
- **後勝ちオーバーライドで全画面ポリッシュ**（style.css 末尾に「高級感ポリッシュ」セクションを追加）
  - ヘッダーグラデーション強化＋影 / 閉じる・各種ボタンをグラデ＋シャドウ＋ホバー浮上
  - クエスト: カード白化＋影、プログレスバーグラデ、報酬ピル、完了グラデ
  - 事務所: 建物に浮遊アニメ＋ドロップシャドウ、応援力・施設・拡張カードの白化＋影
  - プレイヤーカード: ヘッダーを紫グラデカードに（名前白・称号ゴールド系）、行・チップの白化
  - ジョブ / ギャラリー / Canvas / 痛バッグメニュー / 外部連携: カード・タブ・ボタン統一
- **テスト**: `node --check`（script.js / gameData.js）構文OK、CSS波括弧 584/584 均衡、ブラウザ実機確認を推奨

---

## 2026-08-14（19）

### UI改善: タブ整理・効果音全廃止・報酬の自作ポップアップ化・絵文字→既存画像
- ユーザー要望: ①下のバーから痛バを外す ②tap to start以外の効果音を全消し ③報酬の alert を自作ポップアップに ④絵文字使用を避け、既存ストア画像を使う
- **痛バタブ削除**: `#tab-bar` から痛バッグを除去（タブは5つに）。PCデスクトップの痛バッグ制作アプリは残す。`TAB_MAP` / `TAB_RENDER` からも itabag を削除
- **効果音全廃止**: `playTapSound()` を no-op 化（全呼び出し箇所は残置）。設定画面の「効果音」トグルを削除（BGMのみ）。タイトル画面の「tap to start」音は残す
- **報酬ダイアログ（自作ポップアップ）**: `#game-dialog`（中央カード・アイコン/タイトル/報酬行/OK・背景クリックでも閉じる、z-index 700）を追加。`showGameDialog` / `closeGameDialog` / `rewardLine` を実装
  - 置換対象（alert → ダイアログ）: クエスト報酬（通貨/EXP/レベルアップ行を個別表示）/ ショップ購入 / ギャラリー投稿 / 壁飾り / 売上回収 / ギャラリー購入 / ジョブ解放 / ジョブLvアップ
  - バリデーション・エラー系の alert（通貨不足・売約済み・解放条件など）と「準備中」はそのまま
- **絵文字→既存画像**: ボトムタブのショップを `images/pc/Milli Shop.png` に、`popup-shop` ヘッダーも同画像に（`.tab-btn img` / `.popup-header-icon` スタイル追加）。PCアプリは従来どおり既存画像を使用。画像のない項目（ギャラリー/クエスト/事務所タブ・グッズ・ジョブ等）は絵文字を代用継続
- **テスト**: `node --check` 構文OK / ナビゲーション回帰テスト25項目PASS（痛バ除去後もタブ切替・履歴・戻るが正常）

---

## 2026-08-14（18）

### 画面構成を「ボトムタブ + 全画面」方式にリファクタ
- ユーザー要望: 「基本的に全てタブもしくは全画面で一つのものとして機能するように」→ 選択肢「ボトムタブ+全画面（Recommended）」を承認
- **index.html**: 下部に `#tab-bar`（6タブ: 🏠ホーム / 🛒ショップ / 🖼️ギャラリー / 📋クエスト / 🏢事務所 / 🎒痛バッグ、`data-tab` 属性）を追加
- **style.css**:
  - `.popup-overlay` を全画面化（`inset:0`・背景 #f5f2fa・flex column・`padding-bottom:60px`＝タブバー分）
  - `.popup-content` を flex column 全高（max-width 解除）、`.popup-header` をアプリバー化（左右54pxパディングで戻るボタン分を確保）、`.popup-body` をスクロール領域、`.popup-footer` を下部固定
  - `#tab-bar` / `.tab-btn(.active)`（紫 #8b7fd4）/ `#home-screen` に `padding-bottom:60px`
- **script.js**（全画面ナビゲーション化）:
  - `screenHistory` + `TAB_MAP`: タブ画面は履歴を**置き換え**、サブ画面（プレイヤーカード/ジョブ/Canvas/設定/連携/お知らせ）は履歴に**積む**
  - `openPopup(id)`: 開く前に他を全て閉じる（**重ね表示なし・常に1画面**）。タブ画面ならアクティブタブ更新
  - `closePopup()`: 履歴が2以上なら1つ前の画面へ戻る（再pushなし）。1つ以下なら全閉じ→ホーム・homeタブアクティブ
  - `.popup-header` 全画面に戻るボタン（←）を自動注入（`closePopup` バインド）
  - `#tab-bar` クリック配線: home=全閉じ / 他=`TAB_RENDER[tab]()`（事務所は訪問カウント込み）→ `openPopup('popup-'+tab)`
  - `updateTabBarVisibility()`: タイトル / ログインゲート / 初期設定 / PCデスクトップ表示中はタブバー非表示（PCの z-index 300 < タブバー600 のため）。showHomeScreen / openPC / closePC / showLoginGate / hideLoginGate から呼ぶ
  - `openPC` / `closePC` を `window` に公開（タブ配線から使用可能に）
  - ギャラリー→Canvas の遷移から余分な `closePopup` を除去（戻るボタンでギャラリーに戻れるように）
- **テスト**（Node + DOMスタブ、実コード抽出）: 全25項目PASS
  - タブ切替で重ね表示なし / 履歴の置き換え・積み・pop / サブ画面の戻る / ホームへ全閉じ / PC・ゲート・タイトル中のタブバー非表示
- `node --check script.js` 構文OK

---

## 2026-08-14（6）

### アカウント連携（Firebase Auth）でプレイヤーIDを全サイト共通化
- **発見した問題**: localStorage はオリジン単位で分離されるため、別ドメイン（Unishare/Games）から本アプリの `millipro_userdata` を読めない。実 DB は同期しても watchEvents/gameEvents が一切記録されない状態だった
- **方針**: ①Firebase Auth 共通ログイン（全サイト同じプロジェクトのメール+パスワード）でプレイヤーIDを共有 ＋ ②ID持ち込みフォールバック（連携IDの手動入力）
- **データパス**: `millipro/users/{uid}/profile = { playerId, playerName, updatedAt }`（ログイン端末すべてにIDを反映する元）、`millipro/users/{uid}/gamedata`（本アプリのみ同期）
- **本アプリ側（実装済み）**
  - index.html: `firebase-auth-compat.js`（10.12.2 CDN）追加
  - firebase-init.js: `isAuthAvailable` / `getMilliproUid` / `onMilliproAuth` / `milliproLogin` / `milliproSignup` / `milliproLogout` / `ensureMilliproProfile` / `applyMilliproProfile` / `syncMilliproGameData` / `completeMilliproLogin` / `setMilliproPlayerId`
  - script.js: 🔗 連携画面にアカウントセクション（ログイン/新規作成フォーム・連携ID表示+コピー・ログアウト）。Auth 状態変化で再描画
  - `copyMilliproPlayerId`（クリップボード）・`submitMilliproAuth`（モード判定: ログイン/新規作成）
- **連携ハンドオフ.md 更新**: §1-2（localStorage 分離の注意）・§1-4（`setMilliproPlayerId` とID入力UI仕様）・§2-1（auth script）・§2-4（アカウント連携共通仕様・UI最小構成）・§3/§4（アカウントUI追加をやること一覧に）、§6-7（Authentication 有効化手順）
- **テスト**: /tmp/opencode/external_auth_test.js（Auth モックを FB_STUB に追加）
  - 未ログイン時: フォーム表示 / 新規作成: playerId 維持・cloud profile {TEST001} 作成・gamedata アップロード / ログアウト: フォームに戻る / 再ログイン: cloud gamedata（通貨999・v4）を取り込み。JSエラー0件
  - PWA オーバーレイがクリックを遮るため evaluate 方式に変更（Playwright click は不可）

### 注意（引き継ぎ）
- milli-games は recordGameClear 未実装のまま（ハンドオフ資料 §4 の依頼が必要）
- milli-unishare の recordWatch は cheer（応援）時発火で実装済み（視聴完了ではない。仕様差分あり）
- Firebase コンソールで Authentication の「メール/パスワード」を有効化しないとログイン不可（§6-7）→ **有効化済み**（ユーザー確認）

---

## 2026-08-14（17）

### ギャラリー一式を実装（§22: Canvasお絵かき・投稿・展示・売買・壁飾り）
- ユーザー選択: ギャラリー一式（Canvas お絵かき + 画像アップロード + 展示・出品・売買 + 壁飾り）
- **gameData.js**:
  - `GALLERY_SALE_RATE = 0.9`（§18: 販売額の90%が売り手）/ `GALLERY_FIRST_REWARD`（初回投稿 30通貨/15EXP）
  - `addLocalArtwork` / `removeLocalArtwork` / `addOwnedArtwork` / `setWallArtwork` / `getWallArtwork`
  - 日替わりクエスト「ギャラリーに投稿」（progressKey: `galleryPosts`）追加 / stats に `artworksListed` 追加
- **script.js**:
  - `renderCanvasScreen`: Canvas 512×512、色12色・太さ3種・ペン/消しゴム・全消し・端末保存・画像選択（長辺512pxに縮小→JPEG）
  - `renderGalleryScreen`（3タブ: 投稿 / マイギャラリー / みんなのギャラリー）
  - 投稿: タイトル/説明/自分用・展示・出品（価格10〜）/ 初回報酬 + 創作力+5 + クエスト進行
  - マイギャラリー: 自分の作品（公開設定変更・壁飾り・削除）+ 購入した絵（壁飾り）
  - みんなのギャラリー: RTDB `millipro/gallery` を読み込み、作者表示（アイコン判定は連携ハンドオフの `data:image/` 方式）、応援💛、購入はトランザクションで二重購入防止
  - 売上: `millipro/gallerySales/{sellerId}/{artId}` に 90% を記録 → `syncGallerySales()` で回収（人気+10/件、起動時とタブ表示時・外部同期と同パターン）
  - 壁飾り: `renderWallArtwork()` でホームのコレクション棚の壁に額装表示
- **index.html**: `popup-canvas` / `popup-gallery` + PCアプリ「🖼️ ギャラリー」追加 / **style.css**: Canvas・ギャラリー・額装スタイル
- アーキテクチャメモ: 画像は RTDB に base64（512px JPEG で圧縮）で保存。Cloudinary 導入時は `gallerySubmitRemote` の保存先を差し替えるだけで移行可能
- **テスト**（Node 単体）: 作品追加/削除、購入絵追加、壁飾り設定・取得（mine/owned/欠落時null）、クエスト進行。JS構文エラー0件

---

## 2026-08-14（16）

### ジョブ解放・育成UIを実装（§20: Lv5+ポイントで解放、ポイント消費で育成）
- ユーザー選択: ジョブ解放・育成UI（プレイヤーカードの「未解放」表示を操作可能に）
- **gameData.js**:
  - `JOB_UNLOCK_POINTS = 50`（解放に必要なポイント。canUnlockJob の 10 → 50 に変更）
  - `JOB_LEVEL_COST_BASE = 10` + `jobLevelUpCost(level)`: Lv n→n+1 のコスト = 10×n（Lv1→2: 10 / Lv5→6: 50）
  - `unlockJob`（ポイント消費なし）/ `levelUpJob`（ジョブ固有ポイントを消費、`{ ok, cost, newLevel }` を返す）
  - `jobBuffRate` / `applyJobBuff`: 解放済みジョブの Lv合計（Lv1はバフなし）で通貨/EXP +5%/Lv・上限+100%
  - `officeCostDiscountRate`: 応援力系ジョブ（staff/itabag/cheerleader）で事務所拡張コスト -5%/Lv・上限-30%
  - 日替わりクエスト「ジョブを育成」（progressKey: `jobLevelUps`）追加
- **script.js**:
  - `renderJobsScreen()`: 称号表示 + 8ジョブのカード（未解放=条件表示+解放ボタン / 解放済み=Lv・バフ%・育成ボタン）
  - プレイヤーカード: ジョブチップに Lv 表示 + 「🎖️ ジョブを管理する」ボタン → popup-jobs
  - 事務所: 拡張コストに軽減を適用し「XX%OFF」を表示（表示と購入時の両方）
- **dungeon.js**: ダンジョン報酬に `applyJobBuff(gd, 'cheer', ...)` を適用 / **script.js 痛バッグ報酬**にも同バフ
- **index.html**: `popup-jobs` / **style.css**: ジョブカード・バフバッジ・OFF表示スタイル
- **テスト**（Node 単体）: Lv1では解放不可→Lv5+50で解放OK / 育成コストとポイント消費 / 不足拒否 / バフ倍率（Lv2=+5%、Lv10=+45%、丸め 50→53）/ 事務所軽減 / クエスト完了。JSエラー0件
- 設計メモ: バフは「Lv1はバフなし・Lvを上げた分だけ +5%」方式（解放直後に効果が出ると育成の動機が薄れるため）

---

## 2026-08-14（15）

### 痛バッグ制作ミニゲームを実装（§16: 注文通りにバッジ配置のタイムアタック）
- ユーザー選択: ショップに続く次のコンテンツとして痛バッグ制作
- **仕様（企画書 §16 準拠）**: 素材は最初から所持（購入不要）。3つの注文を連続でこなす。各注文60秒。★はタイム+正確さで判定
- **gameData.js**:
  - `ITABAG_BADGES`（5種: 缶バッジ/アクキー/アクスタ/ポスター/バッジパーツ）
  - `ITABAG_REWARDS`（★別）+ `ITABAG_TOTAL_REWARDS`（3注文合計★1〜9、上限 ★3×3 = 150通貨/75EXP）
  - `recordItabag`（collection.itabags + stats.itabagClears）/ `itabagReward` / `itabagTotalReward`
  - 日替わりクエスト「痛バッグを完成」（progressKey: `itabagComplete`）追加
- **script.js**:
  - `renderItabagScreen`（メニュー: ルール・報酬・完成数表示）/ `startItabagGame`（3ラウンド）
  - `renderItabagRound`（注文ランダム生成 4〜6スロット + 60秒カウントダウン、残り10秒で赤点滅）
  - `renderItabagGame`（お手本グリッド / バッグ3×3 / パレット選択→タップ配置・再タップで外す / 完成！判定 / 全部消す）
  - 星評価: 20秒以内+ミス0=★3 / 35秒以内=★2 / それ以外=★1、タイムアップ=0★（次の注文へ）
  - `finishItabagGame`: 合計★で報酬付与 + 記録 + クエスト進行 + レベルアップ演出
  - ポップアップを閉じるとタイマー停止・進行破棄（popup-itabag に直接リスナー）
  - PCデスクトップに「🎒 痛バッグ制作」アプリを追加（ゲームセクション）
- **index.html**: `popup-itabag` / **style.css**: メニュー・ゲーム・パレット・星表示・報酬画面のスタイル
- **テスト**（Node 単体）: 星評価条件・報酬テーブル・記録・クエスト完了・タイムアップ分岐を確認。JSエラー0件
- 発見・修正: 合計★に単品報酬を使うと常に★1扱いになるバグ → 合計用テーブルを新設

---

## 2026-08-14（14）

### ショップ（Milli Shop）を実装: 通貨消費の循環を完成
- ユーザー選択: 「次にすべきことは？」→ ショップ（`SHOP_GOODS` / `collection.goods` が既にあったため最短）
- **gameData.js**:
  - `SHOP_GOODS` に `emoji` / `desc` を追加（コレクション棚・ショップ画面の表示用）
  - `buyGoods(data, goodsId)`: 通貨を消費して `collection.goods` に加算。戻り値 `{ ok, reason: 'currency' | 'notfound' }`
  - `ownedGoodsList(data)`: 所持グッズ一覧（購入回数付き）
  - 日替わりクエストに「ショップでお買い物」（progressKey: `goodsPurchased`）を追加
- **script.js**:
  - `renderShopScreen()`: 残高表示 + 8品目の購入UI（通貨不足時はボタン無効）。購入時はクエスト進行 + 棚更新
  - `renderCollectionShelf()`: ホームのコレクション棚9スロットに所持グッズの絵文字を飾る（購入で自動更新）
  - PCデスクトップの `shop` アプリを「準備中」→ ショップポップアップに接続
- **index.html**: `popup-shop` 追加 / **style.css**: ショップUI + 棚の filled スロット表示
- **テスト**（Node 単体）: 購入成功（100→0・所持+1）/ 通貨不足拒否 / 不明品目拒否 / 複数購入カウント / クエスト完了（shop_buy）まで合格

---

## 2026-08-14（13）

### プレイヤーアイコン機能: 絵文字選択 + 画像アップロード（共通プロフィールと自動連携）
- ユーザー要望: 「アイコンを絵文字だけでなくアップロードも可に」
- **script.js**:
  - `renderUserIcon(el, user)`: アイコン表示の共通ヘルパ。`icon` が `data:image/` で始まる場合は `<img>`、それ以外は絵文字テキスト、未設定なら名前一文字
  - `ICON_EMOJIS`（36種）を追加。`isImageIcon()` で画像データURLを判別
  - プレイヤーカードに「✏️ アイコンを変更」→ アイコンエディタ（絵文字グリッド / 📁 画像を選ぶ / 保存・名前一文字に戻す・キャンセル）
  - `handleIconUpload`: 5MBまで。256pxに正方形クロップして JPEG 0.85 で圧縮し dataURL 化（約10〜20KB・RTDB制限内）
  - `saveIconEditor`: `millipro_userdata.icon` に保存 + `updateMilliproProfile({icon})` でクラウド共有
  - 表示箇所: ステータスバー / PCデスクトップ右上 / プレイヤーカードのアバター
- **firebase-init.js**: `updateMilliproProfile(patch)` を追加。ログイン中なら `millipro/users/{uid}/profile` に部分更新（未ログインなら何もしない）
- **style.css**: アイコンエディタ（プレビュー・絵文字グリッド・アップロード・アクション）とアバター画像の丸切り抜きスタイル
- アイコン機能完成により、以前の「アイコンは空文字で保存」状態は解消（profile.icon に絵文字 or 画像 dataURL が入る）
- **他サイトへの連絡事項**: ハンドオフ §2-4 に「アイコン表示時の画像判別」を追記（Unishare / Games 側は `data:image/` 判定 + `<img>` 描画が必要）

---

## 2026-08-14（12）

### 共通プロフィール拡張: 名前・アイコン・一言をアカウントに紐づけて全サイトで共有
- ユーザー要望: 「アカウントに当サイトでの名前とアイコン（あれば）と一言を紐づけて、他サイトでもそのまま使えるように」
- **firebase-init.js**:
  - profile スキーマを `{ playerId, playerName, icon, comment, updatedAt }` に拡張
  - `ensureMilliproProfile`: 新規作成時にローカルの playerName/icon/comment を引き継ぐ。既存 profile に欠けている項目（icon/comment 等）もローカルから補充
  - `applyMilliproProfile`: playerId に加えて playerName/icon/comment をローカルへ反映（他項目は保持）
- **連携ハンドオフ §2-4**: データパス定義・ログイン成功時の処理・サンプル関数（ensure/apply）を icon/comment 込みに更新。「playerName/icon/comment は自サイトの表示に使ってよい」と明記
- **テスト**（external_auth_test.js）:
  - 新規作成 → cloudProfile に icon:"🌟"・comment:"よろしく"・playerName が保存される
  - 別端末で profile 変更（名前・アイコン・一言）→ 再ログイン → ローカルに反映・playerId は維持（TEST001）。JSエラー0件
  - 既存テスト全て合格（external/quest/office/dungeon）
- アイコンは本アプリに現状機能がないため空文字で保存（機能ができたら自動で紐づく設計）→ **（13）でアイコン機能を実装済み**。以降は絵文字 or 画像 dataURL が保存される

---

## 2026-08-14（11）

### ログインUI改善: タブでログイン/新規登録を分離 + パスワード2回入力 + 表示トグル
- ユーザー要望: ①ログインと新規登録が同じフォームだと「変わってるのかわかりにくい」→ **タブ切替に分離** ②新規登録は**パスワード2回入力** ③パスワードの**表示/非表示トグル**
- **index.html**: login-gate をタブ構造に（`gate-tab-login` / `gate-tab-signup`、`gate-panel-login` / `gate-panel-signup`）。新規登録パネルは `signup-gate-email` / `signup-gate-pass` / `signup-gate-pass2` + 各パスワードにトグルボタン
- **script.js**:
  - `submitMilliproAuth(mode, gateMode)`: タブごとのフィールド ID を使い分け。新規登録は pass1 === pass2 検証（不一致ならエラーで送信中止）+ 6文字未満チェック、`switchAuthTab(tab, gateMode)` + `togglePassVisibility(inputId, btnId)`
  - renderExternalScreen のアカウントUIもタブ化（`auth-tab-*` / `auth-panel-*`、新規登録は `auth2-*` フィールド）
- **style.css**: `.auth-tabs` / `.auth-tab-btn(.active)` / `.password-field` / `.pass-toggle-btn` 追加（ゲート用は白背景に暗色テキストの上書き）
- **テスト**（external_auth_test.js）: パスワード不一致 → 「パスワードが一致しません」+ ゲート開いたまま / トグルで type=text / 一致させて登録成功・ゲート閉じる・playerId 維持 / ログアウト→ゲート再表示 / 再ログイン pull。JSエラー0件。既存テスト（external/quest/office/dungeon）も全て合格
- **ハンドオフ §2-4**: サンプルも同じ UX に更新（mp-tab / mp-panel、mpToggle、パスワード2回検証）

---

## 2026-08-14（10）

### Unishare / Games のログイン方針を確定:「ログイン任意 + 連携案内バナー」
- ユーザー相談の結論: 動画視聴・ミニゲームは**ログインなしで自由に遊べる**（必須にすると離脱が増える）。ただし報酬を受け取るには連携が必要なので、未ログイン & 連携ID未設定のユーザーに「🎁 報酬がもらえます」**案内バナー**を表示してログイン or 連携ID入力を促す
- **連携ハンドオフ.md 更新**:
  - §2-4 仕様: 「ログインしてないユーザー」の扱いを「任意 + 案内バナー」に変更。実装手順に「連携案内バナー追加」を追加（5ステップに）
  - §2-4 に**連携案内バナーのサンプルコードを追加**: `mp-banner`（HTML）+ `mpRefreshBanner` / `mpHideBanner` / `mpOpenAccount`（JS）。表示条件 = ログイン済み or 連携ID設定済みなら非表示
  - §3/§4 の「やること」項目を「アカウント連携UI」→「連携案内バナー」に更新
- 本アプリは従来どおりログイン必須ゲート（セーブデータ拠点）のまま

---

## 2026-08-14（9）

### ログイン必須ゲート（未ログインはゲームを操作できないように）
- ユーザー指摘: 「未ログインでも自由に入れる状態」「ゲートが必要では」→ ログイン必須ゲートを実装
- **index.html**: `login-gate`（全画面オーバーレイ）追加。ログイン/新規作成ボタン・メール/パスワード欄
- **style.css**: `.login-gate`（z-index 999999・暗いグラデ背景・閉じる手段なし）
- **script.js**:
  - `submitMilliproAuth(mode, gateMode)` — ゲート経由時は `login-gate-*` 要素を使用し、成功後にゲートを閉じる
  - `showLoginGate` / `hideLoginGate`
  - `ensureLoginSync(uid)` — completeMilliproLogin（profile保証+playerId反映+gamedata同期）を**1回だけ**実行（並行呼び出しは同じ Promise を共有）。二重実行による「pull なのに保存表示」バグを防止
  - `onMilliproAuth`: uid あり → ゲート閉じる + 同期（pull ならホーム再描画）／uid なし → ゲート表示。**auth 未設定（configなし）の環境ではゲートを出さない**（開発・テスト互換）
- **テスト**（external_auth_test.js 書き換え）: 起動時ゲート表示 → ゲートから新規作成（ゲート閉じる・playerId維持・cloud 保存）→ 外部画面で連携済み確認 → ログアウトでゲート再表示 → ゲートから再ログイン（pull で通貨999・「読み込みました」表示）。JSエラー0件
- 回帰: external（削除検証含む）/ quest / office / dungeon_quest 全テスト合格
- ハンドオフ §5 に「ログイン必須ゲートは本アプリのみ。Unishare/Games は必須にしない」と明記

---

## 2026-08-14（8）

### 無料枠（Spark）の使用量調査 + 消費済みイベントの自動削除を実装
- **クォータ確認**（firebase.google.com/pricing）: RTDB = 同時接続100 / ストレージ1GB / ダウンロード10GB月。Auth メール+パスワードは無料（5万人MAUまで）。電話認証のみ有料（未使用）
- **使用量見積り**: イベント1件 ~60B。想定100人×1日15イベント → 書き込み90KB/日・読み込みは同期頻度依存（1日10回×100人でも27MB/月 ≒ 10GBの0.3%）。ストレージは年32MB（1GBの3%）
- **結論**: 超過しない。Spark は超過しても課金されずリクエスト拒否のみ（Blaze でない限り請求なし）
- **唯一の長期的懸念**: イベントの無制限蓄積 → **対策として消費済みイベント自動削除を実装**（ユーザーに確認済み・推奨選択）
  - `external.js` の `claimMarkerOnce` に deletePath 引数を追加。報酬付与の transaction が committed になったらイベントノードを `remove()`
  - `syncVideoRewards`: `millipro/watchEvents/{pid}/{videoId}/{date}` を削除 / `syncGameRewards`: `millipro/gameEvents/{pid}/{gameId}/{timestamp}` を削除
  - `millipro/rewards/...` マーカーは**残す**（別デバイスからの二重付与防止のため）
- **テスト**（external_test.js）: FB_STUB に remove を追加。CONSUMED CLEANUP 検証で videoDatesLeft=0 / mazeEventsLeft=0（削除OK）・quizMarkers は残存（マーカー維持）・JSエラー0件。既存検証（報酬付与・二重付与なし・1時間窓）も全て合格
- ハンドオフ資料 §7 に「消費済みイベントは本アプリが削除する」仕様を追記（他サイト実装者向け）

---

## 2026-08-14（7）

### ハンドオフ資料を「他リポジトリがそのままコピペできる」形に詳細化
- §2-4 を全面書き換え: 仕様 + 実装手順4ステップ + 認証関数の完成サンプル（isAuthAvailable / onMilliproAuth / milliproLogin / milliproSignup / milliproLogout / ensureMilliproProfile / applyMilliproProfile / completeMilliproLogin）、連携UIの HTML/JS サンプル（mpRender / mpSubmit / mpLogout / mpCopyId / mpSetId）、動作確認手順5ステップ
  - 他サイト版 completeMilliproLogin は gamedata 同期なし（gamedata は本アプリのみ・書き込み禁止と明記）
  - HTML 読込順: app → database → auth の順であることを明記
  - エラー文言対応表（email-already-in-use / wrong-password / weak-password / invalid-email）をサンプルに内蔵
- §1-4: 完成サンプル（mp-id-input / mpSetId）への参照を追記
- §6-7: 「メール/パスワード有効化」に ✅ 済み マーク（ユーザーがコンソールで有効化済み）

---

## 2026-08-14（2）

### 外部連携（Milli Unishare / Milli Games）の受け口を実装 + ハンドオフ資料作成
- **全体設計**（企画書 §13: Firebase 連携）: 3サイトで Firebase Realtime Database を共有
  - Unishare / Games は**イベント記録のみ**（本リポジトリに作業指示書を用意）→ 本アプリが**報酬付与**を担当
  - ユーザー識別は `localStorage.millipro_userdata.playerId` を各サイト共通で使用
  - データパス: `millipro/watchEvents/{pid}/{videoId}/{date}`（動画）、`millipro/gameEvents/{pid}/{gameId}/{timestamp}`（ゲーム）
  - 受取済みマーカー `millipro/rewards/...` を transaction で書き二重付与を防止
- **本アプリ側（実装済み）**
  - `firebase-config.js`: Firebase config 置き場（空のままなら全機能無効・エラーなし）
  - `firebase-init.js`: 遅延初期化 + `getMilliproPlayerId()`（他サイトと共通形式）
  - `external.js`: `syncExternalRewards()` = watchEvents/gameEvents を読み、報酬を付与
    - 動画視聴: 15通貨/8EXP/応援力5（1動画1日1回）
    - ミニゲーム: 10通貨/5EXP（同一ゲーム1時間1回、`gd.externalRewards.gameLastClaimed` で判定）
  - デスクトップに **🔗 連携** アプリを追加（状態表示 + 同期ボタン）、ホーム表示時に自動同期
  - スキーマ v4 に拡張（`externalRewards` 追加、migrate 対応）+ 日替わりクエスト「動画を1本視聴」追加
  - index.html: Firebase SDK（v10 compat CDN）+ 各 JS を読み込み
- **`連携ハンドオフ.md`（仮置き・完了後に削除）**: 他リポジトリ向けの作業指示書
  - ユーザーの Firebase セットアップ手順（プロジェクト作成→RTDB→ルール→config 配布）
  - milli-unishare / milli-games それぞれの変更ファイル・追加コードの形式（サンプルコード付き）
  - コード規約（var/function ベース、トップレベル定数のみ const）等の注意事項

### テスト
- Playwright + Firebase SDK モック（CDN を差し替えて `__fbData` で RTDB を再現）
  - 自動同期で動画+ゲーム報酬が付与（通貨25/EXP13/応援力5、クエスト videosWatched=1）、マーカー書き込みを確認
  - 2回目同期で二重付与なし、新規ゲームイベントのみ +10/+5、1時間以内の同一ゲームはスキップ
  - クエスト画面は6カード（日替わり4 + 週替わり2）に増えたことを確認、JSエラー0件

### TODO（ユーザー作業）
- Firebase セットアップ（連携ハンドオフ.md §6）→ config を各サイトの firebase-config.js に配布
- milli-unishare / milli-games リポジトリに作業を依頼（連携ハンドオフ.md §3・§4）
- 全サイト動作確認後に 連携ハンドオフ.md を削除

---

## 2026-08-14（5）

### ハンドオフ資料に「役割分担」を明確化
- ユーザーから「報酬は各サイトで設定するのか、それともトリガーだけ送って報酬はこっちか」という質問があり、回答+資料化
- 仕様: **報酬の設定・付与は本アプリのみ。他サイト（Unishare/Games）はイベント送信（トリガー）だけ**。報酬額は他サイトのコード/UI に書かない（二重付与・数値ズレ防止）
- 変更点: 連携ハンドオフ.md の §0 に「役割分担の最重要ポイント」の表と説明を追加、§3/§4/§7 にも注意書きを追加
- 報酬変更は本アプリ `external.js` の `EXTERNAL_REWARD` を直すだけで、他サイトの再デプロイは不要

---

## 2026-08-14（4）

### 外部連携: Firebase 実接続の設定・検証と二重付与バグ修正
- ユーザーが Firebase プロジェクト（millipro-shared / RTDB asia-southeast1）を作成し、`firebase-config.js` に config を設定
  - Firebase コンソールのコピーは変数名 `firebaseConfig`（小文字）で出るため、`firebase-init.js` を **`FIREBASE_CONFIG` / `firebaseConfig` 両対応**に修正（ハンドオフ資料のサンプルも同形式に更新）
- **実DB（本番 RTDB）での検証で二重付与バグを発見・修正**
  - 原因: RTDB の `transaction` は「更新関数が同じ参照を返す」だけでは**中断されず committed になる**（実測で 2回とも committed=true）。**`undefined` を返すと中断**される仕様
  - 修正: `claimMarkerOnce` で受取済み時は `return undefined` に変更（コメントで注意事項を明記）
  - 検証: 実DBにテストイベント書込 → 同期で 15通貨/8EXP/応援力5 付与 → 2回目同期で「新しい報酬はありません」→ テストデータ削除、JSエラー0件
- モックテストの transaction スタブも実SDKと同じ中断条件（same-ref / undefined）に修正して再検証済み
- 実DBは検証後クリーン（millipro 配下に残骸なし）を確認

---

## 2026-08-14（3）

### ハンドオフ資料の修正: RTDB のリージョンについて
- `連携ハンドオフ.md` §6 の「場所: asia-northeast1（東京）推奨」は誤りだったため修正
- 調査結果: **Realtime Database の選択肢は 米国(us-central1) / ベルギー(europe-west1) / シンガポール(asia-southeast1) の3つのみ**（東京は Cloud Firestore 側にしかない。公式ドキュメント確認済み）
- 修正内容: 日本からは**シンガポール (asia-southeast1) を選択** + 作成後のリージョン変更不可の注意書きを追記
- ユーザーはシンガポール選択で Firebase セットアップを続行中

---

## 2026-08-14

### クエスト（日替わり・週替わり）を実装
- PCデスクトップのアプリ一覧に **クエスト**（📋）を追加（企画書 §14）
- **データ基盤**（gameData.js、スキーマ v3 へ拡張）
  - `quests.daily` / `quests.weekly` に `claimed`（受取済み）と `progress`（進行度）を追加
  - `migrateGameData` で旧スキーマ（v2）からの統合に対応（date/week/done/claimed を維持、progress は新規）
  - 日替わり: `todayKey()`（YYYY-MM-DD）でリセット、週替わり: `weekKey()`（月曜始まり週）でリセット
  - `rollQuests`（リセット判定）/ `questAddProgress`（進行加算+完了判定）/ `claimQuestRewards`（報酬受け取り）を追加
- **クエスト定義**
  - 日替わり: ダンジョン挑戦 1回 / 事務所訪問 1回 / 通貨獲得 100（各 50通貨 + 30EXP）
  - 週替わり: ダンジョンクリア 3回 / 応援力獲得 300（各 200通貨 + 100EXP）
- **進行フック**
  - dungeon.js: `startBattle` で `dungeonTries`、`applyRewards` で `currencyEarned` / `cheerGained` / `dungeonClears` を加算
  - script.js: 事務所アプリ起動時に `officeVisits` を加算（アップグレード再描画では加算しない）
- **クエスト画面**（ポップアップ `popup-quests` / `quests-body`）
  - 日替わり・週替わりセクション + リセット時間表示（あと◯時間◯分）
  - クエストカード: 名称・説明・進捗バー・進行度（◯/◯）・報酬（💰通貨 / EXP）
  - 完了時に「🎁 報酬を受け取る」ボタン → 通貨+EXP 付与（レベルアップ対応）、受取済み表示
  - クエスト完了時は進行度バーが緑に変化

### テスト
- Playwright ヘッドレスで検証（デスクトップ1280x800 / モバイル390x844）
  - v2セーブデータからの移行（progress/claimed 追加）→ クエスト画面描画（5カード）→ 進行フック → 受け取り（通貨300→350、EXP50→80）を確認
  - ダンジョン: 挑戦開始で `dungeonTries=1`、勝利で `currencyEarned=20` / `cheerGained=30` / `dungeonClears=1` を確認
  - モバイル: レイアウト崩れ・JSエラー0件

---

## 2026-08-12（後半）

### 事務所（Office）画面を実装
- PCデスクトップのアプリ一覧に **事務所**（🏢）を追加（データ基盤 §10 の段階制を使用）
- **画面構成**（ポップアップ、`popup-office` / `office-body`）
  - 建物ビジュアル: 段階ごとに変わる絵文字（🏠→🏢→🏬→🎙️→🏟️→🌌）+ 浮遊アニメーション
  - 段階名 / 応援力残高（拡張用通貨）
  - 施設一覧: 6施設（配信室 / 録音ブース / 会議室 / 資料室 / イベントホール / 大型施設）を解放済み/ロックで表示
  - 拡張パネル: 次段階の名称・解放施設・必要応援力・進捗バー・拡張ボタン
  - ミリメンの様子: タレント3人がランダムな状態（配信準備中 / 収録中 / 休憩中 等）で登場
- **拡張処理**: 応援力から必要分を消費 → `office.stage` 加算 → 施設解放 → 保存 → 画面+ステータスバーを再描画
- タップ音は既存の `tap-se` を再利用（`playTapSound`）

### テスト
- Playwright ヘッドレスで検証（デスクトップ1280x800 / モバイル390x844）
  - 事務所アプリ起動 → 画面描画 → 拡張ボタン → 応援力6000→1000、段階1→2、録音ブース解放を確認
  - JSエラー0件

### 配信ダンジョンのバランス調整（レベル上げ速度の改善）
- 背景: 旧報酬（★1勝利=15EXP/回、1時間5回制限）だと Lv1→2 に約1.5時間、Lv10解放まで約12時間かかる設計だった
- **報酬を全ランク強化**（約2.5倍）
  - ★1: 20通貨/15EXP/10応援 → **50/50/25**
  - ★2: 50/35/20 → **120/100/50**
  - ★3: 100/70/40 → **250/180/100**
  - ★4: 180/120/70 → **400/300/180**
  - ★5: 300/200/120 → **650/480/300**
- **敗北時報酬**: 通貨10%+EXP0 → **通貨30%+EXP30%**（応援力は0）に変更。★2以降の挑戦が全く進まない事態を解消
- **制限緩和**: 1時間 5回 → **8回**（UIは DUNGEON_LIMIT 定数を参照しているので自動反映）
- **バグ修正**: applyRewards のクエスト進行で通貨が応援力クエストに二重加算されていた（cheerGained=75 になる）問題を修正

### テスト
- バランス試算: Lv1→2 は勝利2回（約30分）、Lv1→10 は約3時間に改善
- Playwright: ★1勝利で currencyEarned=50 / cheerGained=25（正しい値）を確認、JSエラー0件

### 今後の予定（メモ）
- 動画視聴（Milli Unishare 連携）の報酬受け口
- ショップ（通貨でグッズ購入）
- 事務所: NPCミリメンの動き（SDキャラ歩行）は画像アセットが揃ったら

---

## 2026-08-12

### 配信ダンジョンを「ダンジョンテーマ」に刷新 + 立ち絵対応 + モバイル改善
- **立ち絵対応**: バトル画面のタレント表示を `images/standing/<id>.png`（新規フォルダ）に変更
  - 未配置時は自動で `images/talents/<id>.webp`（丸顔ポートレート）へフォールバック（onerror切替）
  - 選択画面のタレントチップも立ち絵を使用（フォールバック付き）
- **ダンジョン風の演出**（CSSのみ・画像アセット不要）
  - 背景: 石壁テクスチャ（CSSグラデーション）・画面上下の暗幕・ビネット
  - 左右に松明（🔥）+ ゆらめく光背（torchFlicker / glowPulse アニメーション）
  - バトルステージ: 石畳の床（battle-floor）付きの窪み風ステージ
  - タレントは立ち絵が床の上に立つレイアウト（下影付き）
  - ボスは「光背付きエモジ像」（レッドグロー）に変更
  - VSバッジは金色の円形メダリオンに変更
  - ボスカード/タレントチップ/結果ボックスを石版風（線形グラデーション+ダークゴールド縁取り）
- **モバイル修正**（スマホのタレント選択が小さく見づらい問題）
  - タレント選択を横スクロール行 → グリッド表示（3列・チップ拡大）に変更
  - ボスカードもモバイルで1行1〜2枚になるよう調整
  - チャット高さを38vh→34vhに縮め、ステージを優先表示
- 未使用CSS削除（.fighter.boss .fighter-avatar 等）

### テスト
- Playwright ヘッドレスで実レイアウト計測検証（モバイル390x844 / デスクトップ1440x900）
  - 選択画面: チップ111px・3列グリッド・横スクロール廃止を確認
  - バトル: はみ出しなし・立ち絵が床位置に着地・フォールバック動作を確認
  - JSコンソールエラー0件（404は未配置の立ち絵のみ＝フォールバック正常動作の証拠）

### 今後の予定（メモ）
- クエスト（日替わり・週替わり）
- 事務所（Office）画面
- 動画視聴（Milli Unishare 連携）の報酬受け口
- ショップ（通貨でグッズ購入）

---

## 2026-08-11

### 配信ダンジョンを「ライブ配信風」の専用ページ（dungeon.html）に作り直し
- PCデスクトップ「配信ダンジョン」をクリックすると新規タブで **dungeon.html** を開くように変更（旧オーバーレイ版は削除）
- **レイアウト**: YouTubeライブ風の全画面レイアウト
  - ヘッダー: ←戻る / LIVEバッジ（点滅）/ 配信タイトル / 視聴者数 / 所持通貨
  - 左: 配信ステージ（選択 → バトル → 結果）、右: ライブチャット
- **ライブチャット**
  - 自動生成コメントが絶え間なく流れる（ファン名+メッセージプール）
  - 自分のコメント送信も可能（強調表示）
  - バトル行動に連動するリアクションコメント（攻撃/回復/勝利/敗北など）
  - スパチャ・ギフトは金色のスーパーチャット枠で表示
  - 視聴者数・オンライン数の自動変動シミュレーション
- **バトル**: 従来のターン制（攻撃/防御/回復/宝箱）を踏襲
  - テロップ風バトルログ / ダメージポップ / 画面シェイク / HPバー
  - WebAudio で効果音（攻撃・回復・宝箱・勝利・敗北）
  - 配信中は BGM（Mile Stone）を低音量で再生
- **ギフト（通貨消費）を新設**
  - 💡サイリウム 10通貨: 次の攻撃1.5倍
  - 🎀ペンライト 50通貨: 次の攻撃2倍
  - 💰スパチャ 100通貨: 大ダメージ+タレント回復
- **報酬**: 従来と同じ（通貨+EXP+応援力、敗北時は通貨10%）
  - 別ページのため LocalStorage 経由で共有。メイン画面は storage イベントで自動更新
  - 1時間5回制限も維持（挑戦開始時に記録）
- **レスポンシブ**: モバイルは縦並び（上=ステージ / 下=チャット38vh）
- 旧実装（index.html のオーバーレイ / script.js のダンジョン一式 / style.css のダンジョンCSS）を全削除

### テスト
- `node --check` で dungeon.js / script.js / gameData.js の構文チェック
- バトルロジック・ギフト・報酬付与・試行回数制限を DOM スタブ付きスモークテストで検証
- 勝率シミュレーション: 推奨Lvで100%勝利（攻撃+回復 / 宝箱混ぜ戦略）、30ターン上限も確認
- 旧コードの残存参照（openDungeonScreen / dungeonBody 等）が無いことを確認

### 今後の予定（メモ）
- クエスト（日替わり・週替わり）
- 事務所（Office）画面
- 動画視聴（Milli Unishare 連携）の報酬受け口
- ショップ（通貨でグッズ購入）

---

## 2026-08-11（後半）

### 配信ダンジョン（報酬源の最小実装）を実装
- PCデスクトップの「ゲーム」欄に **配信ダンジョン**（⚔️）を追加
- 画面構成: ボス選択 → 挑戦タレント選択 → ターン制バトル → 結果報酬
- **バトル**: ターン制（ポケモン風）
  - コマンド: ⚔️攻撃 / 🛡️防御 / 💊回復 / 📦宝箱（60%大当たり・失敗時は1.3倍被弾）
  - 30ターンでタイムアップ（敗北）
  - バトルログ表示（配信コメント風の演出付き）
- **制限**: 1時間に5回まで（スライディング窓方式・`dungeon.attempts` で記録）
- **ボス**: 5体（寝坊スライムキング / 回線ゴーレム / 締切ドラゴン / バグの魔王 / システムクラッシュ）
  - 推奨Lv（1/10/20/30/40）でロック
  - 勝率検証済み: 推奨Lvでは安定勝利、1つ下のLvでは苦戦（★4〜5は賢い戦略で勝てる）
- **報酬**: ランク別テーブル（通貨+EXP+応援力）。敗北時は通貨10%のみ
  - 報酬付与後、ステータスバー/プレイヤーカードに即反映
- **セーブスキーマ v2**: `dungeon.attempts` を追加（migrateGameDataで自動移行）
- 企画書 §16 報酬テーブルに応援力・敗北時仕様を追記

### テスト
- Node でバトルロジック・試行制限・報酬計算をシミュレーション検証
- 勝率チューニング: 推奨Lvで98〜100%、1つ下で0〜90%（★4/5は戦略次第）
- ローカルHTTPサーバーで全ファイル配信確認（200）

### 今後の予定（メモ）
- クエスト（日替わり・週替わり）
- 事務所（Office）画面
- 動画視聴（Milli Unishare 連携）の報酬受け口
- ショップ（通貨でグッズ購入）

---

## 2026-08-11（前半）

### 企画書の統合・更新（Ver.0.1 → 0.4）
- 正式名称「Milipro Chronicle（ミリプロクロニクル）」を反映
- 旧企画書・企画案（ジョブ / 配信ダンジョン / 図鑑 / 事務所等）と、企画まとめ Ver.0.1（Bloom Mode / Mili Story / Chronicle Gate 等）を統合
- 決定事項を反映:
  - ホスティング: Render（静的サイト）に確定
  - ゲーム内通貨を導入。「応援＝通貨」は廃止し、応援はポイント/EXP制に
  - 動画視聴は Milli Unishare 主体（視聴判定はUnishare側、本アプリは元データ保存）
  - ミニゲームは「推し活関連=本アプリ / 非関連=Milli Games」に分担
  - Mili Story 以外は全て実装する方針（応募版スコープ制限を撤廃）
  - 放置要素を廃し「オンライン推し活シミュレーション」に
  - 事務所は段階制、配信ダンジョンはターン制+1時間5回制限+ボスランク別報酬
  - ジョブはジョブ固有ポイント制、Lv50上限、一次職は基本Lv5+で解放
  - 探索システムを削除、全体イベントは後回し
  - 画像保存は Cloudinary（Firebase Storage が使えないため）
  - 夕霧レイは加入済みとして扱う

### ゲームデータ基盤の実装（新規: gameData.js）
- セーブスキーマ v1 を新規作成（キー: `millipro_gamedata`）
  - 通貨 / Lv+EXP / 5種ポイント（応援力・知識・創作力・音楽力・人気）/ ジョブ8種 / 事務所段階+施設 / コレクション / ギャラリー / 図鑑5種 / 記憶の庭 / クエスト / 記憶の欠片 / 統計
- バランステーブルを定数化（企画書 Ver.0.4 初期案、実装時に調整）
  - ジョブ定義（JOB_DEFS）/ 事務所段階表（OFFICE_STAGES）/ EXPテーブル（expRequiredForLevel）/ ダンジョン報酬（DUNGEON_REWARDS）/ ショップ品目（SHOP_GOODS）
- 関数: `ensureGameData` / `loadGameData` / `saveGameData` / `migrateGameData`（将来のスキーマ移行用） / `addExp`（Lv50上限・レベルアップ処理） / `canUnlockJob`
- 方針: LocalStorage で先行実装し、構造確定後に Firebase へ移行（ゲストデータ引継ぎも想定）
- script.js の `confirmSetup` で `ensureGameData()` を呼び、初回設定時にゲームデータを自動生成

### ホーム画面にステータス表示（プレイヤーカード）を実装
- ホーム画面上部に**ステータスバー**を追加
  - アバター（名前の頭文字）/ 名前 / Lv / ゲーム内通貨 / EXPゲージ+残量表示
  - クリックでプレイヤーカード（ポップアップ）を表示
- **プレイヤーカード**（ポップアップ）を実装
  - 名前 / 称号（上位職判定付き: ミリリス→一次職→マルチクリエイター等）/ ID
  - Lv + EXPバー / ゲーム内通貨 / 5種ポイント / 事務所の現在段階+次段階 / 解放済みジョブ一覧
- PCデスクトップのユーザー情報にも Lv を表示するように変更
- 役割: `refreshPlayerStatus`（ステータスバー更新・グローバル公開）/ `renderPlayerCard`（カード描画）/ `getUpperJobTitle`（上位職判定）
- ユーザー欄クリックの挙動を「準備中アラート」→「プレイヤーカード表示」に変更

### テスト
- `node --check` で構文チェック（script.js / gameData.js / pwa.js）
- Node 上で スキーマ生成・Lvアップ・ジョブ解放判定・セーブ/ロード・上位職判定・EXP% 計算を確認
- ローカルHTTPサーバーで全ファイルの配信を確認（200）

### 今後の予定（メモ）
- 報酬源の最小実装（配信ダンジョン or クエスト）
- 事務所（Office）画面
- 動画視聴（Milli Unishare 連携）の報酬受け口
