# 外部連携（Milli Unishare / Milli Games）ハンドオフ資料 【仮置き・完了後に削除】

> このファイルは仮置き。3リポジトリすべての連携実装が終わって動作確認が取れたら削除する。
> 対象読者: **milli-unishare リポジトリ / milli-games リポジトリで作業するオープンコード（別インスタンス）** と、設定作業をする人。

---

## 0. 全体設計（3サイト共通の Firebase 共有バックエンド）

```
milli-unishare.onrender.com ─┐
milli-games.onrender.com   ─┼──▶ Firebase Realtime Database ──▶ Millipro-Chronicle（本アプリ）
                             │         （イベント記録のみ）           （報酬付与は本アプリの役割）
```

### ⚠️ 役割分担の最重要ポイント（実装前に必ず理解する）

| | 何を書く / 持つ | 何を一切持たない |
|---|---|---|
| **milli-unishare / milli-games** | 「〇〇をした」という**イベント記録だけ** | **報酬の数値・設定・付与ロジックを持たない** |
| **本アプリ（Millipro-Chronicle）** | 報酬テーブル・付与ロジック・受取済みマーカー | イベントの発生判定を持たない |

- **報酬額は本アプリ側（`external.js` の `EXTERNAL_REWARD`）だけに存在する**。他サイトには通貨/EXP などの数値を**コードにも UI にも書かない**こと。
  - 例: 「動画を最後まで見て +15通貨」といった文言・定数を Unishare 側に置かない。送るのは「この動画を視聴し終えた」という事実のみ。
- **報酬を変更したくなったら本アプリの `EXTERNAL_REWARD` を直すだけでよい**（他サイトの再デプロイは不要）。
- 逆に、他サイト側に報酬を持たせると**二重付与の混乱や数値ズレの原因**になるため絶対にしない。
- 他サイトの作業は「イベントを正しく送る」ことだけ。報酬がどうなるかは本アプリの世界の話であり、動作確認では「Firebase にイベントが入る」ことを見ればよい（報酬の適用確認は本アプリ側で行う）。

- ユーザー識別は `localStorage.millipro_userdata.playerId`（各サイト共通の形式。下記 §1）。
- 認証は使わない（開発初期は Firebase ルール全開放。後で絞る）。

### データパス（RTDB ルートは `millipro/`）

| パス | 書くサイト | 内容 |
|---|---|---|
| `millipro/watchEvents/{playerId}/{videoId}/{date}` | milli-unishare | 動画視聴完了イベント。値: `{ watchedAt, durationSec }` |
| `millipro/gameEvents/{playerId}/{gameId}/{timestamp}` | milli-games | ミニゲームクリアイベント。値: `{ score, playedAt }` |
| `millipro/rewards/{playerId}/watch/{videoId}/{date}` | 本アプリのみ | 受取済みマーカー。値: `{ grantedAt }` |
| `millipro/rewards/{playerId}/games/{gameId}/{timestamp}` | 本アプリのみ | 受取済みマーカー。値: `{ grantedAt }` |

- `{date}` = `YYYY-MM-DD`（ローカル時刻）→ **1動画1日1回** の制限を実現
- 本アプリは「イベントが有る & マーカーが無い」ものだけ報酬を付与 → 二重付与なし

### 報酬テーブル（本アプリ側で適用。企画書 §18・§17 準拠）

| イベント | 通貨 | EXP | 応援力 | 制限 |
|---|---|---|---|---|
| 動画視聴（1本） | 15 | 8 | 5 | 1動画1日1回（Unishare 側で記録抑制） |
| ミニゲームクリア（1回） | 10 | 5 | 0 | 同一ゲーム 1時間1回（本アプリ側で判定） |

---

## 1. 【共通】playerId の取得ヘルパ（3サイトともこの形式で書く）

全サイトは `localStorage.millipro_userdata`（本アプリが保存する形式）を読む:

```js
// firebase-init.js に含める（下記 §2 参照）
function getMilliproPlayerId() {
  try {
    var ud = JSON.parse(localStorage.getItem('millipro_userdata'))
    return ud && ud.playerId ? ud.playerId : null
  } catch (e) { return null }
}
```

- ゲストプレイ中などで `playerId` が無い場合は **イベントを書かない**（null なら return）。

### 1-2. ⚠️ 重要: localStorage はドメイン（オリジン）ごとに分離される

- `localStorage` は「ブラウザ共通」ではなく**サイトごと**に分かれる。本アプリで作った `millipro_userdata` は、別ドメインの Unishare / Games からは**直接読めない**。
- そのため各サイトで playerId が揃うようにする仕組みが**2つ**ある（下記 §1-3 と §1-4）。**両方実装すること**（片方だけだと想定外の環境で壊れる）。

### 1-3. アカウント連携（Firebase Auth・推奨）

3サイトが**同じ Firebase プロジェクトの Auth（メール+パスワード）**を使う。ログインしたユーザーの playerId を各サイトの localStorage に反映する（詳細 §2-4）。

### 1-4. 連携IDの手動入力（ログイン不要のフォールバック）

`getMilliproPlayerId()` が `null` のとき、各サイトの UI に「連携ID入力欄」を出し、保存する:

```js
// firebase-init.js に含める（setMilliproPlayerId も共通）
function setMilliproPlayerId(id) {
  var ud = null
  try { ud = JSON.parse(localStorage.getItem('millipro_userdata')) } catch (e) {}
  if (!ud || typeof ud !== 'object') ud = { createdAt: Date.now() }
  ud.playerId = String(id)
  ud.updatedAt = Date.now()
  localStorage.setItem('millipro_userdata', JSON.stringify(ud))
  return ud
}
```

- 実装例（Unishare / Games 側）: ヘッダーや設定に「連携ID」入力フォームを置く。入力されたら `setMilliproPlayerId(入力値)` → 以後イベントがその ID で送られる。
- 本アプリの 🔗 連携画面に「連携IDをコピー」ボタンがあるので、ユーザーはそこからコピーして貼り付けられる。

---

## 2. 【共通】Firebase 初期化（3サイトすべてに同じ構成）

### 2-1. HTML に SDK（v9 compat、CDN）と config を読み込む

各サイトの `index.html` の `</body>` 直前などに追加（ビルド不要。既存サイトは素の HTML+JS 前提）:

```html
<script src="https://www.gstatic.com/firebasejs/10.12.2/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.12.2/firebase-database-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.12.2/firebase-auth-compat.js"></script>
<script src="firebase-config.js"></script>
<script src="firebase-init.js"></script>
```

### 2-2. `firebase-config.js`（新規ファイル・各サイトに置く）

```js
// Firebase コンソール → プロジェクト設定 → マイアプリ → ウェブアプリ からコピーして貼り付ける
// （コンソールのコピーは変数名 firebaseConfig で出てくる。そのまま貼り付けてOK。
//   firebase-init.js が FIREBASE_CONFIG / firebaseConfig の両方に対応している）
const firebaseConfig = {
  apiKey: '',
  authDomain: '',
  databaseURL: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
}
```

### 2-3. `firebase-init.js`（新規ファイル・各サイトに置く）

```js
// Firebase 初期化（config 未設定なら何もしない）
var firebaseReady = false
function getFirebaseConfig() {
  if (typeof FIREBASE_CONFIG !== 'undefined' && FIREBASE_CONFIG) return FIREBASE_CONFIG
  if (typeof firebaseConfig !== 'undefined' && firebaseConfig) return firebaseConfig
  return null
}
function initFirebase() {
  if (firebaseReady || typeof firebase === 'undefined') return
  var cfg = getFirebaseConfig()
  if (!cfg || !cfg.apiKey || !cfg.databaseURL) return
  firebase.initializeApp(cfg)
  firebaseReady = true
}
function getMilliproPlayerId() {
  try {
    var ud = JSON.parse(localStorage.getItem('millipro_userdata'))
    return ud && ud.playerId ? ud.playerId : null
  } catch (e) { return null }
}
// 連携IDの手動設定（ログイン不要フォールバック用。§1-4）
function setMilliproPlayerId(id) {
  var ud = null
  try { ud = JSON.parse(localStorage.getItem('millipro_userdata')) } catch (e) {}
  if (!ud || typeof ud !== 'object') ud = { createdAt: Date.now() }
  ud.playerId = String(id)
  ud.updatedAt = Date.now()
  localStorage.setItem('millipro_userdata', JSON.stringify(ud))
  return ud
}
initFirebase()
```

### 2-4. アカウント連携（Firebase Auth）の共通仕様

- **ユーザー種別**: 3サイトすべて同じ Firebase プロジェクトの Auth（メール+パスワード）を使用（データベースと同プロジェクト）。
- **データパス**:
  - `millipro/users/{uid}/profile` = `{ playerId, playerName, updatedAt }` — **ログインした端末すべてが playerId を共有するための元データ**
  - `millipro/users/{uid}/gamedata` = ゲームデータ（**本アプリのみ同期**。Unishare / Games にはゲームデータが無いので触らない）
- **ログイン成功時の処理（各サイト共通・この順番）**:
  1. `millipro/users/{uid}/profile` を読み、**無ければ作成**（playerId は端末のローカル値があればそれ、無ければ新規生成）
  2. profile の `playerId` / `playerName` を**自サイトの localStorage.millipro_userdata に反映**（`applyMilliproProfile` 相当。既存項目は保持し playerId だけ上書き）
  3. 以後は通常どおり `getMilliproPlayerId()` でイベント送信
- **UI 仕様（Unishare / Games 側・最小構成で可）**: ヘッダーなどに「アカウント連携」ボタン → メール/パスワードの入力（ログイン + 新規作成）→ 成功時は localStorage 反映。実装は [本アプリの firebase-init.js をそのまま流用可能](本リポジトリ `firebase-init.js` の `isAuthAvailable` / `milliproLogin` / `milliproSignup` / `milliproLogout` / `ensureMilliproProfile` / `applyMilliproProfile` / `completeMilliproLogin` 参照)。
- **ログインしてないユーザー**: イベントは playerId が無い限り送られない。その場合は §1-4 の連携ID入力欄を案内する。
- **注意**: `gamedata` 同期・報酬付与は本アプリの仕事。Unishare / Games は profile の反映だけ行う。

---

## 3. 【作業】milli-unishare リポジトリ

### やること（合計2ファイル + UI追加）

1. `index.html`: §2-1 の script 5行を追加（auth 含む）
2. `firebase-config.js` / `firebase-init.js`: §2 の内容で新規作成
3. 既存の動画プレイヤーコードに視聴完了イベント送信を追加（下記）
4. **アカウント連携UI**（§2-4）: 「アカウント連携」ボタン + ログイン/新規作成フォーム + **連携ID入力欄**（§1-4。`getMilliproPlayerId()` が無い時に案内）を追加

### 視聴完了イベントの送信（既存コードへの追記形式）

YouTube iframe API の場合 `onStateChange` の `state === YT.PlayerState.ENDED`（または再生時間が50%超えた時点）で:

```js
function recordWatch(videoId, durationSec) {
  initFirebase()
  var pid = getMilliproPlayerId()
  if (!firebaseReady || !pid || !videoId) return
  var date = new Date()
  var mm = String(date.getMonth() + 1).padStart(2, '0')
  var dd = String(date.getDate()).padStart(2, '0')
  var dateKey = date.getFullYear() + '-' + mm + '-' + dd
  // 1動画1日1回: 既に記録があれば上書きしない
  firebase.database().ref('millipro/watchEvents/' + pid + '/' + videoId + '/' + dateKey)
    .set({ watchedAt: Date.now(), durationSec: durationSec || 0 })
    .catch(function (e) { console.warn('watchEvent write failed', e) })
}
```

- 同じブラウザで何度再生しても `set` が上書きするだけなので1日1回になる（記録があるかどうか事前確認は任意）。
- `console.warn` で失敗を握りつぶし、サイトの通常動作を壊さないこと。
- **このサイトに報酬の数値を書かない**（§0 の役割分担参照）。報酬は本アプリ側のみ。

### 動作確認方法（Unishare 側）

- ローカル起動 → 動画を最後まで再生 → Firebase コンソールで `millipro/watchEvents/{playerId}/...` にデータが入ることを確認。

---

## 4. 【作業】milli-games リポジトリ

### やること（合計2ファイル + UI追加）

1. `index.html`: §2-1 の script 5行を追加（auth 含む）
2. `firebase-config.js` / `firebase-init.js`: §2 の内容で新規作成
3. 各ミニゲームのクリア判定箇所にイベント送信を追加（下記）
4. **アカウント連携UI**（§2-4）: 「アカウント連携」ボタン + ログイン/新規作成フォーム + **連携ID入力欄**（§1-4）を追加

### クリアイベントの送信

各ミニゲームの「クリア画面を出す」関数内で（例: `showResult(score)` や `onClear()` の先頭）:

```js
function recordGameClear(gameId, score) {
  initFirebase()
  var pid = getMilliproPlayerId()
  if (!firebaseReady || !pid || !gameId) return
  firebase.database().ref('millipro/gameEvents/' + pid + '/' + gameId + '/' + Date.now())
    .set({ score: score || 0, playedAt: Date.now() })
    .catch(function (e) { console.warn('gameEvent write failed', e) })
}
```

- `gameId` はサイト内で一意な文字列（例: `'maze'`, `'puzzle'`, `'reflex'` など、アルファベット小文字・ハイフン推奨）。
- タイムアタック系はスコアが高いほど良い場合と低いほど良い場合がある。送信は `score` のみで、報酬量は一律。**本アプリ側はスコアを見ずにクリア回数で報酬を出す**ので、スコア送信は将来のランキング用。
- **このサイトに報酬の数値を書かない**（§0 の役割分担参照）。報酬は本アプリ側のみ。

### 動作確認方法（Games 側）

- ローカル起動 → ゲームを1回クリア → Firebase コンソールで `millipro/gameEvents/{playerId}/...` にデータが入ることを確認。

---

## 5. 【作業済み】Millipro-Chronicle（本アプリ）

本アプリ側は**このコミットで実装済み**。確認だけしてほしい:

- `firebase-config.js`: **ユーザーが Firebase の config をここに入れる**（§6 の手順）
- `firebase-init.js` / `external.js`: 同期処理（未設定なら何もしないので安心）
- デスクトップに **🔗 連携** アプリを追加。開くと同期状況を表示し「同期する」ボタンで実行
- アプリ起動時にも自動で同期（config 設定済みのときだけ）
- 付与対象: `watchEvents`（15通貨/8EXP/応援力5）、`gameEvents`（10通貨/5EXP・同一ゲーム1時間1回）
- クエスト: 日替わりに「動画を1本視聴」を追加（`videosWatched` 進行キー。Unishare 連携分が加算される）
- 受取済みマーカーは `millipro/rewards/...` に書き込み、二重付与を防止
- 🔗 連携画面に **アカウント連携**（Firebase Auth）と **連携IDコピー** を追加（§2-4）。ログインで playerId をクラウド保存し、別サイトでも同一 playerId を再現。`gamedata` のクラウド保存/読込は本アプリのみ

### 未対応（将来）
- 記憶の欠片（§15）の動画側取得: Unishare 側実装 + 本アプリ受け口が必要（今回の範囲外）
- 図鑑進捗の連携（§13: 「視聴で図鑑発見」も Unishare 側実装）

---

## 6. 【ユーザーがやること】Firebase セットアップ手順

1. **プロジェクト作成**: https://console.firebase.google.com → 「プロジェクトを追加」（プロジェクト名: 例 `millipro-shared`、Google アナリティクスはオフでOK）
2. **Realtime Database 作成**: 左メニュー「Realtime Database」→「データベースを作成」
   - 場所: RTDB で選べるのは **米国 / ベルギー / シンガポール の3択のみ**（東京は Firestore にしかない）。**日本からはシンガポール（asia-southeast1）を選択**
   - 注意: 作成後のリージョン変更は不可（この用途ではどれを選んでも実害なし）
   - ルール: まず **テストモード**（30日制限あり。本格運用までOK）
3. **ルールを開く**: データベース → ルールタブ → 以下を設定（開発中は全開放）:
   ```json
   {
     "rules": {
       ".read": true,
       ".write": true
     }
   }
   ```
   ※実運用時は playerId 単位の制限に絞る（後日対応）
4. **ウェブアプリを登録**: プロジェクト設定 → マイアプリ → `</>` ウェブアプリ → 「ミリクロ共通」で作成
5. **config をコピー**: 表示される `firebaseConfig` を **3リポジトリすべての `firebase-config.js`** に貼り付ける（各サイトに1つずつコピー。Firebase のウェブ設定は公開してよい）
6. **動作確認**:
   - Unishare で動画視聴 → 本アプリの 🔗 連携 → 同期 → 「動画視聴報酬 1件（💰15 / ⭐8 / 応援力5）」が入る
   - Games でクリア → 同期 → ミニゲーム報酬が入る
7. **Authentication を有効化**（アカウント連携を使う場合）: Firebase コンソール → 「Authentication」→「開始する」→ ログイン方法タブ → **メール/パスワード** を有効化（パスワードなしメールリンクは不要）
8. 全て確認できたらこのハンドオフファイル（`連携ハンドオフ.md`）を削除する

---

## 7. イベント/報酬の仕様まとめ（実装者が見るクイックリファレンス）

```
パス:            millipro/watchEvents/{playerId}/{videoId}/{date}
値:             { watchedAt: ms, durationSec: sec }
書き込み:        milli-unishare（視聴完了時・1日1回）
報酬（本アプリ）: 15通貨 / 8EXP / 応援力5

パス:            millipro/gameEvents/{playerId}/{gameId}/{timestamp}
値:             { score: int, playedAt: ms }
書き込み:        milli-games（クリア時）
報酬（本アプリ）: 10通貨 / 5EXP（同一 gameId は1時間1回）

マーカー:        millipro/rewards/{playerId}/watch/{videoId}/{date}
                 millipro/rewards/{playerId}/games/{gameId}/{timestamp}
書き込み:        本アプリのみ（受取済み）
```

- **各サイトの役割は「イベントを送る」だけ。報酬の数値・設定は本アプリ側（`external.js` の `EXTERNAL_REWARD`）にのみ存在する。** 他サイトのコード/UI に報酬額を書かないこと

- 既存サイト（Unishare/Games）のレイアウトや機能は変更しない。**イベント送信は `console.warn` で握りつぶし、通常動作を壊さない**こと
- 各サイトの JS は **本アプリのコード規約に合わせる**: 変数・関数は `var` / `function`（アロー関数・`let` は使わない）。トップレベルの定数だけ `const` 可
- `millipro_gamedata` / `millipro_userdata` の localStorage キーと playerId は本アプリの発行形式に従うこと
